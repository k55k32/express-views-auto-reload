{
  "name": "views-auto-reload",
  "version": "1.0.4",
  "description": "a express middleware that can auto reflush your web page when the files changed",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src -d ./",
    "deploy": "git add . && git commit -m auto-publish && npm version patch && git push origin && npm publish"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/k55k32/express-views-auto-reload.git"
  },
  "keywords": [
    "auto-realod",
    "auto-reflush",
    "express-auto-reflush"
  ],
  "author": "diamond zhou",
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/k55k32/express-views-auto-reload/issues"
  },
  "homepage": "https://github.com/k55k32/express-views-auto-reload#readme",
  "dependencies": {
    "node-watch": "^0.4.1",
    "ws": "^1.1.1"
  },
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-es2015": "^6.18.0",
    "babel-preset-stage-2": "^6.18.0"
  }
}
